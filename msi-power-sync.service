[Unit]
Description=Sync Ubuntu Power Profiles with MSI EC Shift Mode
After=power-profiles-daemon.service

[Service]
Type=simple
ExecStart=/usr/local/bin/msi-power-sync
Restart=always

[Install]
WantedBy=multi-user.target
